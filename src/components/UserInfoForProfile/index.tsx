import { Avatar, Box } from '@mui/material';
import React from 'react';

interface UserInfoForProfileProps {}

const UserInfoForProfile: React.FC<UserInfoForProfileProps> = () => {
  return (
    <Box>
    </Box>
  );
};

export default UserInfoForProfile;
